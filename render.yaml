services:
  - type: web
    name: walletflow
    runtime: node
    plan: free
    buildCommand: npm ci --build-from-source=sqlite3
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
